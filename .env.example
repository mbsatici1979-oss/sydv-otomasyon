# Local server port
PORT=8091

# Temporary password for the first admin user on a fresh installation.
# Change this before first run and do not commit a real .env file.
INITIAL_ADMIN_PASSWORD=change-this-before-first-run
